# Handoff prompt — Relative Sell Model (paste as the first message of a new session)

You are picking up the Relative Sell Model in `/Users/amardani/Sell-Model` — a
sector neutral sell/underperformance model for IMA, a student driven ~20 name
fund picking from the S&P 600 (S&P 400 graduates are monitored, never picked).
It is modeled on the Piper Sandler Sell Model (deck: ~/Downloads/PiperSellModel3Q2026.pdf).
Read `README.md` first; deep context lives in the auto loaded memory
(`project-sell-model-ima.md`), `docs/ic-improvement-roadmap.md`,
`docs/override-layer-design.md`.

## How to work with me (Amar)
I am learning quant finance — explain concepts in plain language with the
research references behind them. Push back on my ideas immediately and hard.
Before code changes: articulate a concise plan, get my approval, then execute
at your discretion. Flag anything removable/wrong with file:line pointers and
let me judge. No hyphens in visible UI text (house style).

## Current state (all committed on main, deployed to imasellmodel.vercel.app)
- Pipeline: `python3 main.py` (~2 min warm). Monthly cross sections back to
  2010 (~199), labels 1Q/2Q forward, Newey West lags scale with overlap.
  Traded sleeves + QoQ comparisons use the 67 quarter ends.
- Fundamentals: SEC EDGAR XBRL companyfacts is the DEFAULT source
  (`edgar_loader.py`, no key, User-Agent only, filing date as of stamps,
  first filed values, ~99.5% parse rate, cached 24h in
  `data/edgar_fundamentals.parquet`). yfinance is per name fallback.
- 19 factors in 6 families (Valuation, Momentum incl 52w high, Volatility =
  IVOL/MAX/beta, Quality, Investment, Earnings Quality). Amihud is torpedo
  only (illiquidity is a return premium — wrong direction for a sell model).
- Peer groups are (date, sector, index): 600 never ranked against 400. All
  headline stats on the S&P 600 selection universe.
- **Default scorer = walk forward ridge (`score_ml`), PROMOTED** over the
  family balanced equal weight baseline via paired IC t test at a one sided
  5% bar (t ≥ 1.645; observed 1.88). DISCLOSURE: bar was relaxed from 2.0
  AFTER seeing 1.88 — PM decision, documented in config.py, Methodology tab,
  commit 52e967f. Never hide this.
- Headline numbers (2011–2026, 183 full factor quarters): learned IC +0.027
  (t=2.8), spread +0.050 (t=5.4, mono −0.92), screen value added +0.84%/yr,
  MC P(beat unscreened) 61/66/92% by tier. Baseline equal weight IC ≈ −0.007:
  valuation flags WORK (P/E t=3.1), quality/accruals INVERTED (t≈−2 to −2.6)
  — the 2020–21 junk rally craters Quality/Volatility (see "What broke" chart).
  Stress windows: protective in COVID crash (+0.105), weak in 2022 rate
  shock (−0.056).
- Data integrity gate excludes splice artifacts (CHRD etc., 64 labels,
  logged in exclusions.json). Placebo diagnostic tests the DEFAULT scorer.
- Dashboard (webapp/, Vite+React+Plotly): tabs Overview, Portfolio Overlay,
  Torpedo, Validation/Backtest, Sector Deciles, Factor IC, Methodology.
  Every ticker click opens a drill down (factor waterfall, formulas, risks
  section draft with gated base rates, analyst override form). Era labels are
  the strings "full factor"/"price only" shared backend↔frontend.
- Analyst override layer: data/overrides.csv (annotations, never touch the
  score, scored quarterly on the Validation tab).

## Immediate next steps (in order)
1. **WRDS**: creds are in `.env` (gitignored) + `~/.pgpass`; `wrds_loader.py`
   connects DIRECTLY via SQLAlchemy (the wrds package prompts interactively —
   don't use it). Live test reached WRDS but login failed ("PAM
   authentication failed") — Amar must verify the web login at
   wrds-www.wharton.upenn.edu and/or ask the WRDS rep to enable pgdata/API
   access for his account. Then `python3 wrds_loader.py` should print
   delisting events. After that: (a) map CRSP dlstcd classes to terminal
   returns (2xx merger → actual dlret, usually premium; 5xx dropped →
   negative), replacing the flat −100% `DELISTING_TERMINAL_RETURN`; (b) PIT
   S&P 600 membership via `comp.idxcst_his` (index constituents history) to
   kill the survivorship banner; (c) cross check EDGAR vs Compustat quarterly.
   LICENSE: Amar has decided WRDS may inform the dashboard (educational use).
   Recorded recommendation in wrds_loader.py: aggregates fine; do NOT ship
   per name raw WRDS data on the public site without access control.
2. **Roadmap 1.5 horizon term structure**: per family IC at 1M/1Q/2Q/4Q from
   existing data ("IC decay curves") — decides whether quality's inversion is
   a speed problem and whether to split fast/slow sleeves.
3. **Roadmap 1.6 walk forward IC weighted family blending** (shrunk trailing
   family ICs as weights — the transparent middle ground between equal weight
   and ridge).
4. Then roadmap 2.3 FINRA short interest history, 2.4 EDGAR Form 4 insiders,
   2.5 price based SUE (earnings day reaction + drift).

## Operational notes
- Caches are 24h TTL (`config.CACHE_MAX_AGE_SECONDS`); `touch data/*.parquet`
  keeps a <1 day old cache warm to avoid re downloads. Prices+volume from
  2010 (`PRICE_HISTORY_START`), benchmark cache separate.
- Webapp: `cd webapp && npm run build` (tsc must stay clean). Preview via
  .claude/launch.json (vite pinned to 127.0.0.1:5173 strict). Deploy:
  `cd webapp && vercel --prod --yes` (project ima_sell_model; verify
  https://imasellmodel.vercel.app/meta.json generated_at afterwards). Commit
  regenerated webapp/public/data/*.json with code changes.
- `python3 main.py --synthetic` = deterministic no network smoke test; must
  stay green (planted signal recovered, placebo collapses).
- Terminal summary prints promotion decision, era split, stress results —
  read it before trusting any run.
